warning: in the working copy of 'src/App.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/App.jsx b/src/App.jsx[m
[1mindex fe7220c..3bca9fb 100644[m
[1m--- a/src/App.jsx[m
[1m+++ b/src/App.jsx[m
[36m@@ -32,6 +32,10 @@[m [mimport {[m
   getSalesToday,[m
   getSalesThisWeek,[m
   getSalesThisMonth,[m
[32m+[m[32m  getSalesByDate,[m
[32m+[m[32m  getTopProducts,[m
[32m+[m[32m  getVentasFiadoPendientes,[m
[32m+[m[32m  marcarFiadoCobrado,[m
   sumSales,[m
   groupSalesByDay,[m
   salesByMethod,[m
[36m@@ -363,6 +367,7 @@[m [mfunction MainApp({ user }) {[m
     { id: "pos",       label: "Punto de Venta", icon: ShoppingCart },[m
     { id: "dashboard", label: "Dashboard",       icon: BarChart3    },[m
     { id: "inventory", label: "Inventario",      icon: Package      },[m
[32m+[m[32m    { id: "fiado",     label: "Fiado",           icon: Clock        },[m
   ];[m
 [m
   return ([m
[36m@@ -416,6 +421,8 @@[m [mfunction MainApp({ user }) {[m
           <POSView products={products} sales={sales} user={user} />[m
         ) : view === "dashboard" ? ([m
           <DashboardView products={products} sales={sales} />[m
[32m+[m[32m        ) : view === "fiado" ? ([m
[32m+[m[32m          <FiadoView />[m
         ) : ([m
           <InventoryView products={products} />[m
         )}[m
[36m@@ -441,6 +448,9 @@[m [mfunction POSView({ products, sales, user }) {[m
   const [ticket,          setTicket]          = useState(null);   // datos del ticket a imprimir[m
   const [processing,      setProcessing]      = useState(false);[m
   const [saleError,       setSaleError]       = useState("");[m
[32m+[m[32m  const [fiadoMode,       setFiadoMode]       = useState(false);[m
[32m+[m[32m  const [fiadoNombre,     setFiadoNombre]     = useState("");[m
[32m+[m[32m  const [fiadoTelefono,   setFiadoTelefono]   = useState("");[m
 [m
   const filteredProducts = useMemo(() => products.filter((p) => {[m
     const matchSearch = p.nombre?.toLowerCase().includes(search.toLowerCase()) ||[m
[36m@@ -494,33 +504,34 @@[m [mfunction POSView({ products, sales, user }) {[m
   // ── Confirmar venta — usa completeSale de services.js ──────[m
   const confirmSale = async () => {[m
     setSaleError("");[m
[32m+[m[32m    if (fiadoMode) {[m
[32m+[m[32m      if (!fiadoNombre.trim()) { setSaleError("El nombre del cliente es obligatorio para fiar."); return; }[m
[32m+[m[32m      if (!fiadoTelefono.trim()) { setSaleError("El teléfono del cliente es obligatorio para fiar."); return; }[m
[32m+[m[32m    }[m
     setProcessing(true);[m
     try {[m
[31m-      const result = await completeSale([m
[31m-        cart,[m
[31m-        cartTotal,[m
[31m-        paymentMethod,[m
[31m-        user?.email ?? null[m
[31m-      );[m
[32m+[m[32m      const method = fiadoMode ? "fiado" : paymentMethod;[m
[32m+[m[32m      const fiadoInfo = fiadoMode ? { nombre: fiadoNombre.trim(), telefono: fiadoTelefono.trim() } : null;[m
[32m+[m[32m      const result = await completeSale(cart, cartTotal, method, user?.email ?? null, fiadoInfo);[m
 [m
       if (result.success) {[m
[31m-        // Datos para el ticket de impresión[m
         const ticketData = {[m
[31m-          items:       cart,[m
[31m-          total:       cartTotal,[m
[31m-          metodoPago:  paymentMethod,[m
[31m-          cajero:      user?.email,[m
[31m-          cambio:      paymentMethod === "efectivo" && change > 0 ? change : 0,[m
[31m-          ventaId:     result.ventaId,[m
[32m+[m[32m          items:      cart,[m
[32m+[m[32m          total:      cartTotal,[m
[32m+[m[32m          metodoPago: method,[m
[32m+[m[32m          cajero:     user?.email,[m
[32m+[m[32m          cambio:     !fiadoMode && paymentMethod === "efectivo" && change > 0 ? change : 0,[m
[32m+[m[32m          ventaId:    result.ventaId,[m
[32m+[m[32m          esVentaFiada: fiadoMode,[m
[32m+[m[32m          clienteNombre: fiadoMode ? fiadoNombre : null,[m
         };[m
[31m-[m
[31m-        // Cerrar modal de venta y resetear estado[m
         setSaleModal(false);[m
         setAmountPaid("");[m
         setPaymentMethod("efectivo");[m
[32m+[m[32m        setFiadoMode(false);[m
[32m+[m[32m        setFiadoNombre("");[m
[32m+[m[32m        setFiadoTelefono("");[m
         clearCart();[m
[31m-[m
[31m-        // Mostrar ticket imprimible[m
         setTicket(ticketData);[m
       }[m
     } catch (err) {[m
[36m@@ -629,13 +640,21 @@[m [mfunction POSView({ products, sales, user }) {[m
             <span className="text-orange-400 font-black text-xl">{formatCurrency(cartTotal)}</span>[m
           </div>[m
           <button[m
[31m-            onClick={() => { setSaleError(""); setSaleModal(true); }}[m
[32m+[m[32m            onClick={() => { setSaleError(""); setFiadoMode(false); setSaleModal(true); }}[m
             disabled={cart.length === 0}[m
             className="w-full bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-400 hover:to-orange-500 disabled:opacity-30 disabled:cursor-not-allowed text-white font-bold rounded-xl py-3 text-sm transition-all shadow-lg shadow-orange-500/20 hover:-translate-y-0.5 flex items-center justify-center gap-2"[m
           >[m
             <Zap className="w-4 h-4" />[m
             Cobrar {formatCurrency(cartTotal)}[m
           </button>[m
[32m+[m[32m          <button[m
[32m+[m[32m            onClick={() => { setSaleError(""); setFiadoMode(true); setSaleModal(true); }}[m
[32m+[m[32m            disabled={cart.length === 0}[m
[32m+[m[32m            className="w-full bg-zinc-800 hover:bg-amber-500/20 border border-zinc-700 hover:border-amber-500/50 disabled:opacity-30 disabled:cursor-not-allowed text-amber-400 font-bold rounded-xl py-2.5 text-sm transition-all flex items-center justify-center gap-2"[m
[32m+[m[32m          >[m
[32m+[m[32m            <Clock className="w-4 h-4" />[m
[32m+[m[32m            Fiar[m
[32m+[m[32m          </button>[m
         </div>[m
       </div>[m
 [m
[36m@@ -650,9 +669,14 @@[m [mfunction POSView({ products, sales, user }) {[m
           setAmountPaid={setAmountPaid}[m
           change={change}[m
           onConfirm={confirmSale}[m
[31m-          onClose={() => { setSaleModal(false); setSaleError(""); }}[m
[32m+[m[32m          onClose={() => { setSaleModal(false); setSaleError(""); setFiadoMode(false); setFiadoNombre(""); setFiadoTelefono(""); }}[m
           processing={processing}[m
           error={saleError}[m
[32m+[m[32m          fiadoMode={fiadoMode}[m
[32m+[m[32m          fiadoNombre={fiadoNombre}[m
[32m+[m[32m          setFiadoNombre={setFiadoNombre}[m
[32m+[m[32m          fiadoTelefono={fiadoTelefono}[m
[32m+[m[32m          setFiadoTelefono={setFiadoTelefono}[m
         />[m
       )}[m
 [m
[36m@@ -823,13 +847,15 @@[m [mfunction CartItem({ item, onUpdate, onRemove }) {[m
 }[m
 [m
 // ─── Sale Modal ────────────────────────────────────────────[m
[31m-function SaleModal({ cart, total, paymentMethod, setPaymentMethod, amountPaid, setAmountPaid, change, onConfirm, onClose, processing, error }) {[m
[32m+[m[32mfunction SaleModal({ cart, total, paymentMethod, setPaymentMethod, amountPaid, setAmountPaid, change, onConfirm, onClose, processing, error, fiadoMode, fiadoNombre, setFiadoNombre, fiadoTelefono, setFiadoTelefono }) {[m
   return ([m
     <div className="fixed inset-0 z-50 flex items-center justify-center p-4" onClick={onClose}>[m
       <div className="absolute inset-0 bg-black/70 backdrop-blur-sm" />[m
       <div className="relative bg-[#1a1a1a] border border-zinc-700/60 rounded-2xl p-6 w-full max-w-md shadow-2xl" onClick={(e) => e.stopPropagation()}>[m
         <div className="flex items-center justify-between mb-5">[m
[31m-          <h3 className="text-white font-bold text-lg">Confirmar Venta</h3>[m
[32m+[m[32m          <h3 className="text-white font-bold text-lg flex items-center gap-2">[m
[32m+[m[32m            {fiadoMode ? <><Clock className="w-5 h-5 text-amber-400" /> Registrar como Fiado</> : "Confirmar Venta"}[m
[32m+[m[32m          </h3>[m
           <button onClick={onClose} className="text-zinc-500 hover:text-white transition-colors"><X className="w-5 h-5" /></button>[m
         </div>[m
 [m
[36m@@ -852,42 +878,67 @@[m [mfunction SaleModal({ cart, total, paymentMethod, setPaymentMethod, amountPaid, s[m
         </div>[m
 [m
         {/* Total */}[m
[31m-        <div className="flex justify-between items-center mb-5 bg-orange-500/10 border border-orange-500/20 rounded-xl px-4 py-3">[m
[31m-          <span className="text-orange-300 font-semibold">Total a Cobrar</span>[m
[31m-          <span className="text-orange-400 font-black text-2xl">{formatCurrency(total)}</span>[m
[32m+[m[32m        <div className={`flex justify-between items-center mb-5 border rounded-xl px-4 py-3 ${fiadoMode ? "bg-amber-500/10 border-amber-500/20" : "bg-orange-500/10 border-orange-500/20"}`}>[m
[32m+[m[32m          <span className={`font-semibold ${fiadoMode ? "text-amber-300" : "text-orange-300"}`}>Total {fiadoMode ? "a Fiar" : "a Cobrar"}</span>[m
[32m+[m[32m          <span className={`font-black text-2xl ${fiadoMode ? "text-amber-400" : "text-orange-400"}`}>{formatCurrency(total)}</span>[m
         </div>[m
 [m
[31m-        {/* Método de pago */}[m
[31m-        <div className="mb-4">[m
[31m-          <p className="text-zinc-400 text-xs font-semibold uppercase tracking-wider mb-2">Método de Pago</p>[m
[31m-          <div className="grid grid-cols-3 gap-2">[m
[31m-            {["efectivo", "tarjeta", "transferencia"].map((m) => ([m
[31m-              <button[m
[31m-                key={m} onClick={() => setPaymentMethod(m)}[m
[31m-                className={`py-2 px-3 rounded-xl text-xs font-bold capitalize transition-all ${[m
[31m-                  paymentMethod === m ? "bg-orange-500 text-white shadow-lg shadow-orange-500/30" : "bg-zinc-800 text-zinc-400 hover:bg-zinc-700"[m
[31m-                }`}[m
[31m-              >{m}</button>[m
[31m-            ))}[m
[31m-          </div>[m
[31m-        </div>[m
[31m-[m
[31m-        {/* Calculadora de cambio */}[m
[31m-        {paymentMethod === "efectivo" && ([m
[31m-          <div className="mb-5 space-y-2">[m
[31m-            <p className="text-zinc-400 text-xs font-semibold uppercase tracking-wider">Monto Recibido</p>[m
[32m+[m[32m        {/* Fiado: datos del cliente */}[m
[32m+[m[32m        {fiadoMode ? ([m
[32m+[m[32m          <div className="space-y-3 mb-5 bg-amber-500/5 border border-amber-500/20 rounded-xl p-4">[m
[32m+[m[32m            <p className="text-amber-400 text-xs font-semibold uppercase tracking-wider flex items-center gap-1.5">[m
[32m+[m[32m              <User className="w-3.5 h-3.5" /> Datos del Cliente (Obligatorio)[m
[32m+[m[32m            </p>[m
[32m+[m[32m            <input[m
[32m+[m[32m              type="text"[m
[32m+[m[32m              placeholder="Nombre completo *"[m
[32m+[m[32m              value={fiadoNombre}[m
[32m+[m[32m              onChange={(e) => setFiadoNombre(e.target.value)}[m
[32m+[m[32m              className="w-full bg-zinc-900 border border-amber-700/50 text-white placeholder-zinc-500 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-amber-500/60 transition-all"[m
[32m+[m[32m            />[m
             <input[m
[31m-              type="number" value={amountPaid} onChange={(e) => setAmountPaid(e.target.value)}[m
[31m-              placeholder="0.00"[m
[31m-              className="w-full bg-zinc-900 border border-zinc-700 text-white rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-orange-500/60 transition-all"[m
[32m+[m[32m              type="tel"[m
[32m+[m[32m              placeholder="Teléfono *"[m
[32m+[m[32m              value={fiadoTelefono}[m
[32m+[m[32m              onChange={(e) => setFiadoTelefono(e.target.value)}[m
[32m+[m[32m              className="w-full bg-zinc-900 border border-amber-700/50 text-white placeholder-zinc-500 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-amber-500/60 transition-all"[m
             />[m
[31m-            {amountPaid && ([m
[31m-              <div className={`flex justify-between px-4 py-2.5 rounded-xl ${change >= 0 ? "bg-emerald-500/10 border border-emerald-500/20" : "bg-red-500/10 border border-red-500/20"}`}>[m
[31m-                <span className={`text-sm font-semibold ${change >= 0 ? "text-emerald-400" : "text-red-400"}`}>Cambio</span>[m
[31m-                <span className={`font-black ${change >= 0 ? "text-emerald-400" : "text-red-400"}`}>{formatCurrency(change >= 0 ? change : 0)}</span>[m
[32m+[m[32m          </div>[m
[32m+[m[32m        ) : ([m
[32m+[m[32m          <>[m
[32m+[m[32m            {/* Método de pago */}[m
[32m+[m[32m            <div className="mb-4">[m
[32m+[m[32m              <p className="text-zinc-400 text-xs font-semibold uppercase tracking-wider mb-2">Método de Pago</p>[m
[32m+[m[32m              <div className="grid grid-cols-3 gap-2">[m
[32m+[m[32m                {["efectivo", "tarjeta", "transferencia"].map((m) => ([m
[32m+[m[32m                  <button[m
[32m+[m[32m                    key={m} onClick={() => setPaymentMethod(m)}[m
[32m+[m[32m                    className={`py-2 px-3 rounded-xl text-xs font-bold capitalize transition-all ${[m
[32m+[m[32m                      paymentMethod === m ? "bg-orange-500 text-white shadow-lg shadow-orange-500/30" : "bg-zinc-800 text-zinc-400 hover:bg-zinc-700"[m
[32m+[m[32m                    }`}[m
[32m+[m[32m                  >{m}</button>[m
[32m+[m[32m                ))}[m
[32m+[m[32m              </div>[m
[32m+[m[32m            </div>[m
[32m+[m
[32m+[m[32m            {/* Calculadora de cambio */}[m
[32m+[m[32m            {paymentMethod === "efectivo" && ([m
[32m+[m[32m              <div className="mb-5 space-y-2">[m
[32m+[m[32m                <p className="text-zinc-400 text-xs font-semibold uppercase tracking-wider">Monto Recibido</p>[m
[32m+[m[32m                <input[m
[32m+[m[32m                  type="number" value={amountPaid} onChange={(e) => setAmountPaid(e.target.value)}[m
[32m+[m[32m                  placeholder="0.00"[m
[32m+[m[32m                  className="w-full bg-zinc-900 border border-zinc-700 text-white rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-orange-500/60 transition-all"[m
[32m+[m[32m                />[m
[32m+[m[32m                {amountPaid && ([m
[32m+[m[32m                  <div className={`flex justify-between px-4 py-2.5 rounded-xl ${change >= 0 ? "bg-emerald-500/10 border border-emerald-500/20" : "bg-red-500/10 border border-red-500/20"}`}>[m
[32m+[m[32m                    <span className={`text-sm font-semibold ${change >= 0 ? "text-emerald-400" : "text-red-400"}`}>Cambio</span>[m
[32m+[m[32m                    <span className={`font-black ${change >= 0 ? "text-emerald-400" : "text-red-400"}`}>{formatCurrency(change >= 0 ? change : 0)}</span>[m
[32m+[m[32m                  </div>[m
[32m+[m[32m                )}[m
               </div>[m
             )}[m
[31m-          </div>[m
[32m+[m[32m          </>[m
         )}[m
 [m
         {/* Error */}[m
[36m@@ -902,10 +953,10 @@[m [mfunction SaleModal({ cart, total, paymentMethod, setPaymentMethod, amountPaid, s[m
           <button onClick={onClose} className="flex-1 bg-zinc-800 hover:bg-zinc-700 text-zinc-300 font-semibold rounded-xl py-3 text-sm transition-colors">Cancelar</button>[m
           <button[m
             onClick={onConfirm}[m
[31m-            disabled={processing || (paymentMethod === "efectivo" && amountPaid && change < 0)}[m
[31m-            className="flex-1 bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-400 hover:to-orange-500 disabled:opacity-40 text-white font-bold rounded-xl py-3 text-sm transition-all shadow-lg shadow-orange-500/30 flex items-center justify-center gap-2"[m
[32m+[m[32m            disabled={processing || (!fiadoMode && paymentMethod === "efectivo" && amountPaid && change < 0)}[m
[32m+[m[32m            className={`flex-1 ${fiadoMode ? "bg-gradient-to-r from-amber-500 to-amber-600 hover:from-amber-400 hover:to-amber-500 shadow-amber-500/30" : "bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-400 hover:to-orange-500 shadow-orange-500/30"} disabled:opacity-40 text-white font-bold rounded-xl py-3 text-sm transition-all shadow-lg flex items-center justify-center gap-2`}[m
           >[m
[31m-            {processing ? <RefreshCw className="w-4 h-4 animate-spin" /> : <><Check className="w-4 h-4" /> Confirmar</>}[m
[32m+[m[32m            {processing ? <RefreshCw className="w-4 h-4 animate-spin" /> : <><Check className="w-4 h-4" />{fiadoMode ? "Registrar Fiado" : "Confirmar"}</>}[m
           </button>[m
         </div>[m
       </div>[m
[36m@@ -964,67 +1015,90 @@[m [mfunction ProductDetailModal({ product, onClose, onAdd }) {[m
 // DASHBOARD VIEW — Gráficas SVG puras (sin recharts)[m
 // ============================================================[m
 function DashboardView({ products, sales: salesToday }) {[m
[31m-  const [period,    setPeriod]    = useState("week"); // today | week | month[m
[32m+[m[32m  const [period,    setPeriod]    = useState("today");[m
   const [salesData, setSalesData] = useState([]);[m
   const [loading,   setLoading]   = useState(true);[m
[32m+[m[32m  // Calendar date picker[m
[32m+[m[32m  const todayStr = new Date().toISOString().slice(0, 10);[m
[32m+[m[32m  const [selectedDate, setSelectedDate] = useState(todayStr);[m
[32m+[m[32m  const [calMode, setCalMode] = useState(false); // false = período, true = fecha exacta[m
 [m
   useEffect(() => {[m
     setLoading(true);[m
[31m-    const fetcher =[m
[31m-      period === "today" ? getSalesToday  :[m
[31m-      period === "week"  ? getSalesThisWeek :[m
[31m-                           getSalesThisMonth;[m
[31m-    fetcher()[m
[31m-      .then(setSalesData)[m
[31m-      .finally(() => setLoading(false));[m
[31m-  }, [period]);[m
[32m+[m[32m    let fetcher;[m
[32m+[m[32m    if (calMode) {[m
[32m+[m[32m      const dateObj = new Date(selectedDate + "T12:00:00"); // evitar desfase de zona horaria[m
[32m+[m[32m      fetcher = () => getSalesByDate(dateObj);[m
[32m+[m[32m    } else {[m
[32m+[m[32m      fetcher = period === "today" ? getSalesToday : period === "week" ? getSalesThisWeek : getSalesThisMonth;[m
[32m+[m[32m    }[m
[32m+[m[32m    fetcher().then(setSalesData).finally(() => setLoading(false));[m
[32m+[m[32m  }, [period, calMode, selectedDate]);[m
 [m
   const chartData  = groupSalesByDay(salesData);[m
   const byMethod   = salesByMethod(salesData);[m
   const totalSales = sumSales(salesData);[m
   const avgTicket  = salesData.length > 0 ? totalSales / salesData.length : 0;[m
[32m+[m[32m  const topProds   = getTopProducts(salesData, 6);[m
 [m
   const lowStock  = products.filter((p) => p.stock > 0  && p.stock <= 5);[m
   const outStock  = products.filter((p) => p.stock === 0);[m
 [m
   const kpis = [[m
[31m-    { label: "Total Ventas",    value: formatCurrency(totalSales), icon: DollarSign,  color: "orange", sub: `${salesData.length} transacciones` },[m
[31m-    { label: "Ticket Promedio", value: formatCurrency(avgTicket),  icon: TrendingUp,  color: "blue",   sub: "Por venta" },[m
[31m-    { label: "Stock Bajo",      value: lowStock.length,            icon: AlertTriangle,color: "yellow", sub: "Productos ≤ 5 unidades" },[m
[31m-    { label: "Sin Stock",       value: outStock.length,            icon: Package,      color: "red",    sub: "Agotados" },[m
[32m+[m[32m    { label: "Total Ventas",    value: formatCurrency(totalSales),     icon: DollarSign,   color: "orange", sub: `${salesData.length} transacciones` },[m
[32m+[m[32m    { label: "Ticket Promedio", value: formatCurrency(avgTicket),       icon: TrendingUp,   color: "blue",   sub: "Por venta" },[m
[32m+[m[32m    { label: "Fiado",           value: formatCurrency(byMethod.fiado), icon: Clock,        color: "amber",  sub: "Pendiente de cobro" },[m
[32m+[m[32m    { label: "Sin Stock",       value: outStock.length,                 icon: Package,      color: "red",    sub: "Agotados" },[m
   ];[m
 [m
   const colorMap = {[m
     orange: { bg: "bg-orange-500/10", border: "border-orange-500/20", icon: "text-orange-400", text: "text-orange-400" },[m
     blue:   { bg: "bg-blue-500/10",   border: "border-blue-500/20",   icon: "text-blue-400",   text: "text-blue-400"   },[m
[31m-    yellow: { bg: "bg-yellow-500/10", border: "border-yellow-500/20", icon: "text-yellow-400", text: "text-yellow-400" },[m
[32m+[m[32m    amber:  { bg: "bg-amber-500/10",  border: "border-amber-500/20",  icon: "text-amber-400",  text: "text-amber-400"  },[m
     red:    { bg: "bg-red-500/10",    border: "border-red-500/20",    icon: "text-red-400",    text: "text-red-400"    },[m
   };[m
 [m
[32m+[m[32m  const displayLabel = calMode[m
[32m+[m[32m    ? new Date(selectedDate + "T12:00:00").toLocaleDateString("es-DO", { weekday: "long", year: "numeric", month: "long", day: "numeric" })[m
[32m+[m[32m    : new Date().toLocaleDateString("es-DO", { weekday: "long", year: "numeric", month: "long", day: "numeric" });[m
[32m+[m
   return ([m
     <div className="p-4 lg:p-6 max-w-7xl mx-auto space-y-6">[m
[31m-      {/* Header + selector de período */}[m
[32m+[m[32m      {/* Header + selectores */}[m
       <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3">[m
         <div>[m
           <h2 className="text-white font-black text-2xl">Dashboard</h2>[m
           <p className="text-zinc-500 text-sm mt-0.5 flex items-center gap-1.5">[m
[31m-            <Clock className="w-3.5 h-3.5" />[m
[31m-            {new Date().toLocaleDateString("es-DO", { weekday: "long", year: "numeric", month: "long", day: "numeric" })}[m
[32m+[m[32m            <Clock className="w-3.5 h-3.5" />{displayLabel}[m
           </p>[m
         </div>[m
[31m-        <div className="flex gap-2">[m
[31m-          {[[m
[31m-            { id: "today", label: "Hoy"       },[m
[31m-            { id: "week",  label: "Semana"     },[m
[31m-            { id: "month", label: "Este Mes"   },[m
[31m-          ].map(({ id, label }) => ([m
[31m-            <button[m
[31m-              key={id} onClick={() => setPeriod(id)}[m
[31m-              className={`px-3 py-1.5 rounded-xl text-xs font-bold transition-all ${[m
[31m-                period === id ? "bg-orange-500 text-white" : "bg-zinc-800 text-zinc-400 hover:bg-zinc-700"[m
[31m-              }`}[m
[31m-            >{label}</button>[m
[31m-          ))}[m
[32m+[m[32m        <div className="flex flex-wrap gap-2 items-center">[m
[32m+[m[32m          {/* Date picker */}[m
[32m+[m[32m          <div className="flex items-center gap-2 bg-zinc-900 border border-zinc-700 rounded-xl px-3 py-1.5">[m
[32m+[m[32m            <Calendar className="w-4 h-4 text-zinc-400 shrink-0" />[m
[32m+[m[32m            <input[m
[32m+[m[32m              type="date"[m
[32m+[m[32m              value={selectedDate}[m
[32m+[m[32m              max={todayStr}[m
[32m+[m[32m              onChange={(e) => { setSelectedDate(e.target.value); setCalMode(true); }}[m
[32m+[m[32m              className="bg-transparent text-white text-xs focus:outline-none cursor-pointer"[m
[32m+[m[32m            />[m
[32m+[m[32m          </div>[m
[32m+[m[32m          {/* Período rápido */}[m
[32m+[m[32m          <div className="flex gap-1">[m
[32m+[m[32m            {[[m
[32m+[m[32m              { id: "today", label: "Hoy"      },[m
[32m+[m[32m              { id: "week",  label: "Semana"    },[m
[32m+[m[32m              { id: "month", label: "Mes"       },[m
[32m+[m[32m            ].map(({ id, label }) => ([m
[32m+[m[32m              <button[m
[32m+[m[32m                key={id} onClick={() => { setPeriod(id); setCalMode(false); setSelectedDate(todayStr); }}[m
[32m+[m[32m                className={`px-3 py-1.5 rounded-xl text-xs font-bold transition-all ${[m
[32m+[m[32m                  !calMode && period === id ? "bg-orange-500 text-white" : "bg-zinc-800 text-zinc-400 hover:bg-zinc-700"[m
[32m+[m[32m                }`}[m
[32m+[m[32m              >{label}</button>[m
[32m+[m[32m            ))}[m
[32m+[m[32m          </div>[m
         </div>[m
       </div>[m
 [m
[36m@@ -1048,9 +1122,8 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
         })}[m
       </div>[m
 [m
[31m-      {/* Gráfica de barras SVG + Métodos de pago */}[m
[32m+[m[32m      {/* Gráfica + Métodos de pago */}[m
       <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">[m
[31m-        {/* Barras: ventas por día */}[m
         <div className="lg:col-span-2 bg-zinc-900/60 border border-zinc-800/60 rounded-2xl p-4">[m
           <div className="flex items-center gap-2 mb-4">[m
             <Activity className="w-4 h-4 text-orange-400" />[m
[36m@@ -1060,7 +1133,6 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
           <BarChartSVG data={chartData} />[m
         </div>[m
 [m
[31m-        {/* Métodos de pago */}[m
         <div className="bg-zinc-900/60 border border-zinc-800/60 rounded-2xl p-4">[m
           <div className="flex items-center gap-2 mb-4">[m
             <Tag className="w-4 h-4 text-orange-400" />[m
[36m@@ -1071,9 +1143,10 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
           ) : ([m
             <div className="space-y-3 mt-2">[m
               {[[m
[31m-                { label: "Efectivo",       value: byMethod.efectivo,       color: "#22c55e" },[m
[31m-                { label: "Tarjeta",        value: byMethod.tarjeta,        color: "#3b82f6" },[m
[31m-                { label: "Transferencia",  value: byMethod.transferencia,  color: "#a855f7" },[m
[32m+[m[32m                { label: "Efectivo",      value: byMethod.efectivo,      color: "#22c55e" },[m
[32m+[m[32m                { label: "Tarjeta",       value: byMethod.tarjeta,       color: "#3b82f6" },[m
[32m+[m[32m                { label: "Transferencia", value: byMethod.transferencia,  color: "#a855f7" },[m
[32m+[m[32m                { label: "Fiado",         value: byMethod.fiado,         color: "#f59e0b" },[m
               ].map(({ label, value, color }) => {[m
                 const pct = totalSales > 0 ? Math.round((value / totalSales) * 100) : 0;[m
                 return ([m
[36m@@ -1098,14 +1171,39 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
         </div>[m
       </div>[m
 [m
[31m-      {/* Ventas recientes + alertas de inventario */}[m
[32m+[m[32m      {/* Top productos + ventas recientes */}[m
       <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">[m
[32m+[m[32m        {/* Top productos */}[m
[32m+[m[32m        <div className="bg-zinc-900/60 border border-zinc-800/60 rounded-2xl overflow-hidden">[m
[32m+[m[32m          <div className="px-4 py-3 border-b border-zinc-800/60 flex items-center gap-2">[m
[32m+[m[32m            <Flame className="w-4 h-4 text-orange-400" />[m
[32m+[m[32m            <span className="text-white font-bold text-sm">Productos más vendidos</span>[m
[32m+[m[32m          </div>[m
[32m+[m[32m          <div className="divide-y divide-zinc-800/40">[m
[32m+[m[32m            {topProds.length === 0 ? ([m
[32m+[m[32m              <p className="text-zinc-600 text-sm text-center py-8">Sin datos</p>[m
[32m+[m[32m            ) : topProds.map((p, i) => ([m
[32m+[m[32m              <div key={p.productId} className="px-4 py-3 flex items-center gap-3 hover:bg-zinc-800/20 transition-colors">[m
[32m+[m[32m                <span className={`w-6 h-6 rounded-full flex items-center justify-center text-xs font-black shrink-0 ${i === 0 ? "bg-orange-500/30 text-orange-400" : i === 1 ? "bg-zinc-600/50 text-zinc-400" : "bg-zinc-800 text-zinc-500"}`}>{i+1}</span>[m
[32m+[m[32m                <div className="flex-1 min-w-0">[m
[32m+[m[32m                  <p className="text-white text-sm font-medium truncate">{p.productName}</p>[m
[32m+[m[32m                  <p className="text-zinc-500 text-xs">{p.marca}</p>[m
[32m+[m[32m                </div>[m
[32m+[m[32m                <div className="text-right shrink-0">[m
[32m+[m[32m                  <p className="text-orange-400 font-bold text-sm">{formatCurrency(p.totalRD)}</p>[m
[32m+[m[32m                  <p className="text-zinc-600 text-xs">{p.totalQty > 0 ? `${p.totalQty} ud.` : `${p.totalML}ml`}</p>[m
[32m+[m[32m                </div>[m
[32m+[m[32m              </div>[m
[32m+[m[32m            ))}[m
[32m+[m[32m          </div>[m
[32m+[m[32m        </div>[m
[32m+[m
         {/* Ventas recientes */}[m
         <div className="bg-zinc-900/60 border border-zinc-800/60 rounded-2xl overflow-hidden">[m
           <div className="px-4 py-3 border-b border-zinc-800/60 flex items-center justify-between">[m
             <div className="flex items-center gap-2">[m
               <ShoppingBag className="w-4 h-4 text-orange-400" />[m
[31m-              <span className="text-white font-bold text-sm">Ventas Recientes (hoy)</span>[m
[32m+[m[32m              <span className="text-white font-bold text-sm">Ventas recientes</span>[m
             </div>[m
             <span className="text-zinc-500 text-xs">{salesToday.length} hoy</span>[m
           </div>[m
[36m@@ -1116,7 +1214,10 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
               salesToday.slice(0, 8).map((sale) => ([m
                 <div key={sale.id} className="px-4 py-3 flex items-center justify-between hover:bg-zinc-800/20 transition-colors">[m
                   <div>[m
[31m-                    <p className="text-white text-sm font-medium">{sale.items?.length} ítem{sale.items?.length !== 1 ? "s" : ""}</p>[m
[32m+[m[32m                    <p className="text-white text-sm font-medium flex items-center gap-1.5">[m
[32m+[m[32m                      {sale.items?.length} ítem{sale.items?.length !== 1 ? "s" : ""}[m
[32m+[m[32m                      {sale.esVentaFiada && <span className="text-[10px] bg-amber-500/20 text-amber-400 border border-amber-500/20 px-1.5 py-0.5 rounded-full">Fiado</span>}[m
[32m+[m[32m                    </p>[m
                     <p className="text-zinc-500 text-xs capitalize">{sale.metodoPago} · {sale.cajero?.split("@")[0]}</p>[m
                   </div>[m
                   <div className="text-right">[m
[36m@@ -1130,45 +1231,143 @@[m [mfunction DashboardView({ products, sales: salesToday }) {[m
             )}[m
           </div>[m
         </div>[m
[32m+[m[32m      </div>[m
[32m+[m[32m    </div>[m
[32m+[m[32m  );[m
[32m+[m[32m}[m
 [m
[31m-        {/* Alertas de inventario */}[m
[31m-        <div className="bg-zinc-900/60 border border-zinc-800/60 rounded-2xl overflow-hidden">[m
[31m-          <div className="px-4 py-3 border-b border-zinc-800/60 flex items-center justify-between">[m
[31m-            <div className="flex items-center gap-2">[m
[31m-              <AlertTriangle className="w-4 h-4 text-yellow-400" />[m
[31m-              <span className="text-white font-bold text-sm">Alertas de Inventario</span>[m
[31m-            </div>[m
[31m-            <span className="text-zinc-500 text-xs">{lowStock.length + outStock.length} alertas</span>[m
[32m+[m[32m// ============================================================[m
[32m+[m[32m// FIADO VIEW — Cuentas por Cobrar[m
[32m+[m[32m// ============================================================[m
[32m+[m[32mfunction FiadoView() {[m
[32m+[m[32m  const [fiados,   setFiados]   = useState([]);[m
[32m+[m[32m  const [loading,  setLoading]  = useState(true);[m
[32m+[m[32m  const [marking,  setMarking]  = useState(null);[m
[32m+[m[32m  const [search,   setSearch]   = useState("");[m
[32m+[m
[32m+[m[32m  const load = async () => {[m
[32m+[m[32m    setLoading(true);[m
[32m+[m[32m    try {[m
[32m+[m[32m      const data = await getVentasFiadoPendientes();[m
[32m+[m[32m      setFiados(data);[m
[32m+[m[32m    } catch(err) {[m
[32m+[m[32m      console.error(err);[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      setLoading(false);[m
[32m+[m[32m    }[m
[32m+[m[32m  };[m
[32m+[m
[32m+[m[32m  useEffect(() => { load(); }, []);[m
[32m+[m
[32m+[m[32m  const handleCobrar = async (id) => {[m
[32m+[m[32m    if (!window.confirm("¿Marcar esta deuda como cobrada?")) return;[m
[32m+[m[32m    setMarking(id);[m
[32m+[m[32m    try {[m
[32m+[m[32m      await marcarFiadoCobrado(id);[m
[32m+[m[32m      setFiados((prev) => prev.filter((f) => f.id !== id));[m
[32m+[m[32m    } catch(err) { alert("Error: " + err.message); }[m
[32m+[m[32m    finally { setMarking(null); }[m
[32m+[m[32m  };[m
[32m+[m
[32m+[m[32m  const totalPendiente = fiados.reduce((s, f) => s + (Number(f.total)||0), 0);[m
[32m+[m
[32m+[m[32m  const filtered = fiados.filter((f) =>[m
[32m+[m[32m    (f.clienteNombre || "").toLowerCase().includes(search.toLowerCase()) ||[m
[32m+[m[32m    (f.clienteTelefono || "").includes(search)[m
[32m+[m[32m  );[m
[32m+[m
[32m+[m[32m  return ([m
[32m+[m[32m    <div className="p-4 lg:p-6 max-w-5xl mx-auto space-y-5">[m
[32m+[m[32m      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3">[m
[32m+[m[32m        <div>[m
[32m+[m[32m          <h2 className="text-white font-black text-2xl flex items-center gap-2">[m
[32m+[m[32m            <Clock className="w-6 h-6 text-amber-400" /> Cuentas por Cobrar[m
[32m+[m[32m          </h2>[m
[32m+[m[32m          <p className="text-zinc-500 text-sm mt-0.5">{fiados.length} deudas pendientes</p>[m
[32m+[m[32m        </div>[m
[32m+[m[32m        <div className="flex items-center gap-3">[m
[32m+[m[32m          <div className="bg-amber-500/10 border border-amber-500/25 rounded-xl px-4 py-2.5 text-center">[m
[32m+[m[32m            <p className="text-xs text-amber-400 font-semibold uppercase tracking-wider">Total Pendiente</p>[m
[32m+[m[32m            <p className="text-amber-400 font-black text-xl">{formatCurrency(totalPendiente)}</p>[m
           </div>[m
[31m-          <div className="divide-y divide-zinc-800/40">[m
[31m-            {[...outStock, ...lowStock].length === 0 ? ([m
[31m-              <div className="flex flex-col items-center py-8 gap-2">[m
[31m-                <Check className="w-6 h-6 text-emerald-400" />[m
[31m-                <p className="text-zinc-600 text-sm">Inventario en orden</p>[m
[31m-              </div>[m
[31m-            ) : ([m
[31m-              [...outStock, ...lowStock].slice(0, 8).map((p) => ([m
[31m-                <div key={p.id} className="px-4 py-3 flex items-center justify-between hover:bg-zinc-800/20 transition-colors">[m
[31m-                  <div className="flex items-center gap-2.5">[m
[31m-                    <div className={`w-2 h-2 rounded-full ${p.stock === 0 ? "bg-red-400" : "bg-yellow-400"}`} />[m
[31m-                    <div>[m
[31m-                      <p className="text-white text-sm font-medium">{p.nombre}</p>[m
[31m-                      <p className="text-zinc-500 text-xs">{p.categoria} · {p.marca}</p>[m
[32m+[m[32m          <button onClick={load} className="bg-zinc-800 hover:bg-zinc-700 text-zinc-400 p-2.5 rounded-xl transition-colors">[m
[32m+[m[32m            <RefreshCw className="w-4 h-4" />[m
[32m+[m[32m          </button>[m
[32m+[m[32m        </div>[m
[32m+[m[32m      </div>[m
[32m+[m
[32m+[m[32m      {/* Búsqueda */}[m
[32m+[m[32m      <div className="relative max-w-sm">[m
[32m+[m[32m        <Search className="absolute left-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-zinc-500" />[m
[32m+[m[32m        <input[m
[32m+[m[32m          type="text" value={search} onChange={(e) => setSearch(e.target.value)}[m
[32m+[m[32m          placeholder="Buscar por nombre o teléfono..."[m
[32m+[m[32m          className="w-full bg-zinc-900 border border-zinc-800 text-white placeholder-zinc-600 rounded-xl pl-10 pr-4 py-2.5 text-sm focus:outline-none focus:border-amber-500/50 transition-all"[m
[32m+[m[32m        />[m
[32m+[m[32m      </div>[m
[32m+[m
[32m+[m[32m      {loading ? ([m
[32m+[m[32m        <div className="flex justify-center py-16"><RefreshCw className="w-6 h-6 text-amber-400 animate-spin" /></div>[m
[32m+[m[32m      ) : filtered.length === 0 ? ([m
[32m+[m[32m        <div className="flex flex-col items-center py-16 gap-3">[m
[32m+[m[32m          <Check className="w-12 h-12 text-emerald-400 opacity-50" />[m
[32m+[m[32m          <p className="text-zinc-500 text-sm">{search ? "Sin resultados" : "¡No hay deudas pendientes! 🎉"}</p>[m
[32m+[m[32m        </div>[m
[32m+[m[32m      ) : ([m
[32m+[m[32m        <div className="space-y-3">[m
[32m+[m[32m          {filtered.map((f) => {[m
[32m+[m[32m            const fecha = f.fecha?.toDate?.() ?? new Date(f.fechaISO);[m
[32m+[m[32m            return ([m
[32m+[m[32m              <div key={f.id} className="bg-zinc-900/60 border border-amber-500/20 rounded-2xl p-4 hover:border-amber-500/40 transition-all">[m
[32m+[m[32m                <div className="flex flex-col sm:flex-row sm:items-center gap-4">[m
[32m+[m[32m                  {/* Info cliente */}[m
[32m+[m[32m                  <div className="flex items-center gap-3 flex-1 min-w-0">[m
[32m+[m[32m                    <div className="w-10 h-10 rounded-full bg-amber-500/15 border border-amber-500/25 flex items-center justify-center shrink-0">[m
[32m+[m[32m                      <User className="w-5 h-5 text-amber-400" />[m
[32m+[m[32m                    </div>[m
[32m+[m[32m                    <div className="min-w-0">[m
[32m+[m[32m                      <p className="text-white font-bold truncate">{f.clienteNombre || "—"}</p>[m
[32m+[m[32m                      <p className="text-zinc-400 text-sm">{f.clienteTelefono || "Sin teléfono"}</p>[m
                     </div>[m
                   </div>[m
[31m-                  <span className={`text-xs font-bold px-2.5 py-1 rounded-full ${[m
[31m-                    p.stock === 0[m
[31m-                      ? "bg-red-500/15 text-red-400 border border-red-500/20"[m
[31m-                      : "bg-yellow-500/15 text-yellow-400 border border-yellow-500/20"[m
[31m-                  }`}>[m
[31m-                    {p.stock === 0 ? "Agotado" : `${p.stock} ud.`}[m
[31m-                  </span>[m
[32m+[m
[32m+[m[32m                  {/* Productos */}[m
[32m+[m[32m                  <div className="flex-1 min-w-0">[m
[32m+[m[32m                    <p className="text-zinc-400 text-xs font-semibold mb-1 uppercase tracking-wider">Productos fiados</p>[m
[32m+[m[32m                    <div className="space-y-0.5">[m
[32m+[m[32m                      {(f.items || []).slice(0, 3).map((item, i) => ([m
[32m+[m[32m                        <p key={i} className="text-zinc-300 text-xs truncate">[m
[32m+[m[32m                          {item.esLiquidoDetallado[m
[32m+[m[32m                            ? `${item.productName} (${item.mlAmount}ml) — ${formatCurrency(item.montoRD)}`[m
[32m+[m[32m                            : `${item.quantity}× ${item.productName} — ${formatCurrency(item.subtotal)}`}[m
[32m+[m[32m                        </p>[m
[32m+[m[32m                      ))}[m
[32m+[m[32m                      {(f.items||[]).length > 3 && <p className="text-zinc-600 text-xs">+{f.items.length-3} más...</p>}[m
[32m+[m[32m                    </div>[m
[32m+[m[32m                  </div>[m
[32m+[m
[32m+[m[32m                  {/* Total + acción */}[m
[32m+[m[32m                  <div className="flex items-center gap-3 sm:flex-col sm:items-end">[m
[32m+[m[32m                    <div className="text-right">[m
[32m+[m[32m                      <p className="text-amber-400 font-black text-xl">{formatCurrency(f.total)}</p>[m
[32m+[m[32m                      <p className="text-zinc-600 text-xs">[m
[32m+[m[32m                        {fecha.toLocaleDateString("es-DO", { day: "2-digit", month: "short" })} · {fecha.toLocaleTimeString("es-DO", { hour:"2-digit", minute:"2-digit" })}[m
[32m+[m[32m                      </p>[m
[32m+[m[32m                    </div>[m
[32m+[m[32m                    <button[m
[32m+[m[32m                      onClick={() => handleCobrar(f.id)}[m
[32m+[m[32m                      disabled={marking === f.id}[m
[32m+[m[32m                      className="bg-emerald-500/20 hover:bg-emerald-500/30 border border-emerald-500/30 hover:border-emerald-500/60 text-emerald-400 font-bold px-4 py-2 rounded-xl text-sm transition-all flex items-center gap-2 disabled:opacity-50 shrink-0"[m
[32m+[m[32m                    >[m
[32m+[m[32m                      {marking === f.id ? <RefreshCw className="w-4 h-4 animate-spin" /> : <><Check className="w-4 h-4" /> Cobrado</>}[m
[32m+[m[32m                    </button>[m
[32m+[m[32m                  </div>[m
                 </div>[m
[31m-              ))[m
[31m-            )}[m
[31m-          </div>[m
[32m+[m[32m              </div>[m
[32m+[m[32m            );[m
[32m+[m[32m          })}[m
         </div>[m
[31m-      </div>[m
[32m+[m[32m      )}[m
     </div>[m
   );[m
 }[m
